{
  "host": "${process.env.HOST || 'localhost'}",
  "port": "${process.env.PORT || 1337}",
  "production": true,
  "proxy": {
    "enabled": true
  },
  "cron": {
    "enabled": true
  },
  "admin": {
    "autoOpen": false,
    "path": "/__/cms"
  }
}
